export default function Tags({ title }) {
  return (
    <div className="px-2 py-1 rounded border border-solid border-neutral-grey-lighter bg-magnolia text-center">
      <p className="text-neutral-grey-light text-sm">{title}</p>
    </div>
  );
}
